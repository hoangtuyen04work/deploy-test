server:
  port: 8888
  servlet:
    context-path: /media

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/ig2?useSSL=false"
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
      password: password
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQLDialect
